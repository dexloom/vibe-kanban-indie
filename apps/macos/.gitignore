# XcodeGen output — regenerate with `xcodegen generate`
*.xcodeproj
# Generated Info.plist (produced by XcodeGen from project.yml)
Sources/VibeKanbanMac/Resources/Info.plist

# Staged backend binary (built by scripts/build-backend.sh) — large, not committed.
# Anchored so it does NOT match Sources/VibeKanbanMac/Backend/ (source code).
/Backend/

# Build artifacts
build/
DerivedData/
.build/
*.xcuserstate
xcuserdata/
.DS_Store
